{
  "name": "gamethon-frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/copy-public-assets.js && vite --port 3000 --host 0.0.0.0",
    "build": "node scripts/copy-public-assets.js && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.2.3"
  }
}
